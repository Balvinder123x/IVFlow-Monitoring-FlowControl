services:
  - type: web
    name: ivflow
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 120 --threads 4
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
