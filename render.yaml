services:
  - type: web
    name: ivflow
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120 --threads 4
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
